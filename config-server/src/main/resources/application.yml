server:
  port: 7070
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri:  # ruta del repositorio git
          searchPaths: config-data
          default-label: main
